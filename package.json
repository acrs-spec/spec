{
  "name": "acrs-spec",
  "version": "1.0.0-draft.01",
  "private": true,
  "description": "Agent Commerce Registry Standard (ACRS) — specification, schema, and documentation",
  "scripts": {
    "generate:schema": "tsx scripts/generate-schema.ts",
    "build": "pnpm run generate:schema",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1",
    "ts-json-schema-generator": "^2.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
